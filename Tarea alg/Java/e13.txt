function contarPares(texto) {
    let contador = 0;

    for (let i = 0; i < texto.length; i++) {
        if (i % 2 === 0) { // índice par
            contador++;
        }
    }

    return contador;
}

console.log(contarPares("Hola")); // 2
