function contarCaracteres(texto) {
    let contador = 0;

    for (let i = 0; i < texto.length; i++) {
        contador++; 
    }

    return contador;
}

let texto = "Hola 123!";
console.log("Total de caracteres: " + contarCaracteres(texto));
